# A5 Lesson Learned

Both RUN 1 (success) and RUN 2 (handled error) returned exit code 0, so the status column shows GREEN/SUCCESS for both—exit codes only reflect whether the script *completed*, not whether the logic *succeeded*.